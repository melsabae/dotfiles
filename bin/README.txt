certain commands here are just wrappers around commands that already exist
since this is my personal bin directory and i choose it to be in my PATH before other parts of the path
these run instead of those other commands

the LIBGL_ALWAYS_SOFTWARE=1 bypasses some GPU/CPU issues with this inspiron laptop, since it does not use the manufacturer provided NVIDIA drivers and instead uses open source nouveau

